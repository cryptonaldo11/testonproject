

# Project Memory — backup
> 187 notes | Score threshold: >40

## Safety — Never Run Destructive Commands

> Dangerous commands are actively monitored.
> Critical/high risk commands trigger error notifications in real-time.

- **NEVER** run `rm -rf`, `del /s`, `rmdir`, `format`, or any command that deletes files/directories without EXPLICIT user approval.
- **NEVER** run `DROP TABLE`, `DELETE FROM`, `TRUNCATE`, or any destructive database operation.
- **NEVER** run `git push --force`, `git reset --hard`, or any command that rewrites history.
- **NEVER** run `npm publish`, `docker rm`, `terraform destroy`, or any irreversible deployment/infrastructure command.
- **NEVER** pipe remote scripts to shell (`curl | bash`, `wget | sh`).
- **ALWAYS** ask the user before running commands that modify system state, install packages, or make network requests.
- When in doubt, **show the command first** and wait for approval.

**Stack:** JavaScript/TypeScript · React + Tailwind

## 📝 NOTE: 1 uncommitted file(s) in working tree.\n\n## Important Warnings

- **gotcha in createAlertRequestSeverity.ts** — File updated (external): lib/api-zod/src/generated/types/createAlertRe
- **gotcha in .gitignore** — File updated (external): .gitignore

Content summary (54 lines):
# See

## Project Standards

- what-changed in index.ts — confirmed 5x
- what-changed in tsconfig.tsbuildinfo — confirmed 4x
- what-changed in settings.json — confirmed 6x
- what-changed in settings.json — confirmed 3x

## Known Fixes

- ❌ File updated (external): lib/api-zod/src/generated/types/calculateAllProductivityFailure.ts → ✅ problem-fix in errorResponse.ts

## Recent Decisions

- Optimized impliedFormat — offloads heavy computation off the main thread
- decision in updateLeaveRequest.ts
- decision in createLeaveRequest.ts
- decision in api.ts

## Learned Patterns

- When encountering this, fix by: problem-fix in calculateAllProductivityFailure.ts (seen 2x)
- When encountering this, fix by: problem-fix in errorResponse.ts (seen 2x)

## Available Tools (ON-DEMAND only)
- `query(q)` — Deep search when stuck
- `find(query)` — Full-text lookup
> Context above IS your context. Do NOT call load() at startup.
